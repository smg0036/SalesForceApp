JVM is a specification and charecteristics for runtime environment
JRE is the implementation /actual environment(bytecode and java libraries) that uses byte code generated by compiler/interpreter.

JVM is different for different Operating system

HLL ---> MLL
    I/C

classLoader loads the compiled java code/byte code and also the java libraries 

Interpreter and/or just in compiler is used by JRE to exxecute the program

A smallest unit of your program that can be understood by the computer is a token.tokens in java:
Identifier
Keywords
Operators
Literals

Stack and heap are special memory allocated for a task
Stack--LIFO or FILO
Heap--no particular order of fetching, its random

declaring a variable itself allocates the memory location

Homework: printing a string entered from keyboard